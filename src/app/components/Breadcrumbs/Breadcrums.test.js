import React from 'react';
import { shallow } from 'enzyme';

import Breadcrums from './Breadcrums';

describe('Breadcrums', () => {
    const wrapper = shallow(<Breadcrums />);

    it('', () => {
        //
    });
});
