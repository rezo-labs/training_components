import React from 'react';
import styled from 'styled-components';

const theme = {
    default: ['#015bf7', '#e3e3e3'],
};


export default function Pagination(props) {
    return (
        null
    );
}
